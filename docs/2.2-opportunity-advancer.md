# Process 2.2: Opportunity Advancer - Claude Code Mirror

**Status:** 🚧 Em Especificação
**Prioridade:** 0 (Foundation)
**Tempo Estimado:** 2-3 horas

---

## Objetivo

Criar versão Claude Code do agente **Opportunity Advancer** que já existe no My Notion AI, validando a abordagem híbrida e estabelecendo padrões para futuros agentes.

## Contexto

O Opportunity Advancer (Process 2.2) é o agente mais crítico do workflow de CRM:
- **Função:** Após cada reunião com cliente, atualizar 6 campos da oportunidade com análise estratégica
- **Status no Notion:** ✅ Operacional desde 2025-11-18
- **Comando Notion:** `"Atualize o avanço da oportunidade [Nome]"`
- **Valor:** Garante que pipeline sempre avança após interações, com insights estratégicos e esforço mínimo
- **Upgrade:** Versão Claude Code adiciona campo "AI Advancement Recommendation" com análise multi-interações

**Por que criar espelho Claude Code?**
1. ✅ Versionamento (Git) - rastrear mudanças na lógica do agente
2. ✅ Testing - validar comportamento antes de deploy no Notion
3. ✅ Logging - entender decisões do agente
4. ✅ Foundation - aprender padrões para próximos agentes

---

## Input Specification

### Required Input

**Nome da Oportunidade** (string)
- Exemplo: `"Projeto SiDi - Nova Plataforma"`
- Usado para query no database Oportunidades

### Optional Flags

- `--dry-run`: Mostra output sem atualizar Notion (default: true)
- `--verbose`: Mostra detalhes do processamento (queries, parsing, decisões)
- `--format`: Output format (`markdown` | `json`)

### Comando CLI (Proposto)

```bash
# Dry-run (só mostra resultado, não atualiza)
claude-code advance-opportunity "Projeto SiDi"

# Atualizar de verdade (pede confirmação)
claude-code advance-opportunity "Projeto SiDi" --execute

# Verbose mode
claude-code advance-opportunity "Projeto SiDi" --verbose
```

---

## Processing Logic

### Step 1: Query Oportunidade

**Objetivo:** Encontrar oportunidade no Notion por nome

**MCP Tool:** `mcp__notion__notion-search`

**Query:**
```
query: "nome_da_oportunidade"
query_type: "internal"
data_source_url: "collection://201b1882-308d-4524-8a86-6672d5502299"
```

**Success Criteria:**
- Exatamente 1 resultado encontrado
- Se 0 resultados → Error: "Oportunidade não encontrada"
- Se >1 resultado → Pedir user escolher qual

**Output:** Opportunity page URL

---

### Step 2: Fetch Opportunity Properties

**Objetivo:** Carregar properties atuais da oportunidade

**MCP Tool:** `mcp__notion__notion-fetch`

**Query:**
```
id: opportunity_page_url
```

**Properties to Extract:**
- `Nome Oportunidade` (título da oportunidade)
- `Biz Funnel` (current stage)
- `Status` (current status)
- `Priority` (High/Medium/Low)
- `Update Log` (última entrada)
- `Next Action` (current)
- `NAction Due` (current)
- `Anotações` (relation URLs array)

**Success Criteria:**
- Page loaded com todas properties
- Se Anotações está vazio (sem inline content e sem linked pages) → Error: "Sem dados de reunião para processar"

---

### Step 3: Load Multiple Recent Interactions (Multi-Source Analysis)

**Objetivo:** Carregar 2-3 interações mais recentes para análise estratégica de padrões

**MCP Tool:** `mcp__notion__notion-fetch` (para página completa + Anotações relacionadas)

**Storage Pattern Detection (Priority Order):**
1. **Inline Anotações:** Check page content for `<transcript>`, `<summary>`, `<notes>` tags
2. **Linked Anotações:** Load pages from Anotações relation URLs

**Logic:**
```python
# Pseudo-code
interactions = []

# Pattern 1: Check for inline meeting data in page content
inline_meetings = extract_meetings_from_content(opportunity.page_content)
if inline_meetings:
    for meeting in inline_meetings[:3]:  # Get 2-3 most recent
        interactions.append({
            'type': 'inline',
            'date': extract_date(meeting),
            'transcript': extract_tag(meeting, '<transcript>'),
            'summary': extract_tag(meeting, '<summary>'),
            'notes': extract_tag(meeting, '<notes>')
        })

# Pattern 2: Load linked Anotações pages
if opportunity.anotacoes:
    for url in opportunity.anotacoes[:3]:  # Get 2-3 most recent
        page = fetch(url)
        interactions.append({
            'type': 'linked',
            'url': url,
            'title': page.title,
            'date': page.properties.Date or extract_date_from_title(page.title),
            'transcript': extract_content(page.content, '<transcript>'),
            'summary': extract_content(page.content, '<summary>'),
            'notes': extract_content(page.content, '<notes>')
        })

# Sort by date (most recent first) and limit to 2-3
interactions_sorted = sort_by_date(interactions, descending=True)[:3]
```

**Date Extraction Logic:**
- Primary: `Date` property (if exists)
- Fallback: Parse title format `@Today 11:11 AM (GMT-3)` or `@DD/MM HH:MM AM/PM`
- Last resort: Created timestamp

**Content Sources (Priority Order per interaction):**
1. `<transcript>` tag content (full meeting transcript)
2. `<summary>` tag content (AI-generated summary)
3. `<notes>` tag content (user notes)

**Success Criteria:**
- Pelo menos 1 fonte de conteúdo disponível
- Data da última interação identificada
- 2-3 interações carregadas para análise de padrões (quando disponível)

---

### Step 4: Strategic Analysis Across Multiple Interactions

**Objetivo:** Extrair insights estratégicos e gerar 6 campos com análise de padrões

**AI Processing:** Use Claude's analysis capabilities with multi-source context

**Analysis Prompt Template:**

```markdown
You are analyzing multiple business interactions to update a CRM opportunity with strategic insights.

## Context
- **Opportunity:** {opportunity.Task}
- **Current Biz Funnel Stage:** {opportunity.Biz_Funnel}
- **Current Status:** {opportunity.Status}
- **Priority:** {opportunity.Priority}
- **Last Update Log:** {opportunity.Update_Log (última entrada)}

## Recent Interactions (analyze across all)
{for each interaction in interactions_sorted[:3]}
**Interaction {N}:**
- **Date:** {interaction.date}
- **Type:** {interaction.type (inline/linked/offline)}
- **Transcript:** {interaction.transcript}
- **Summary:** {interaction.summary}
- **Notes:** {interaction.notes}
{end for}

## Task
Based on the interactions above, perform strategic pattern analysis and generate the following 6 fields:

### 1. Update Log (1 concise sentence, max 10 words)
Format: `DD/MM: [key action or result]`

**Strategic Principles:**
- WITH advancement: Log KEY signal/decision
  * Example: `18/11: cliente aprovou proposta técnica`
- WITHOUT advancement: Log strategic CONTEXT/REASON
  * Example: `18/11: cliente aguardando aprovação do board`

Rules:
- Focus on KEY decision, action, result, OR strategic context
- Not a meeting summary, focus on what matters
- Use date of most recent interaction
- Analyze across all interactions to find the most significant update

### 2. Next Action (clear objective action)
Format: Verb + object + minimal context
Example: `Enviar proposta comercial revisada`
Rules:
- Actionable by user (not waiting on client)
- Specific enough to know what to do
- If waiting on client, use Status = "Waiting Feedback" instead

### 3. NAction Due (when to act)
Rules:
- Based on: meeting urgency, client deadlines, proposal timelines
- Default: Last interaction date + 7 calendar days
- If client gave specific deadline, use that
- Format: YYYY-MM-DD

### 4. Biz Funnel (detect stage progression?)
Current stage: {opportunity.Biz_Funnel}
Stages (in order):
1. Marketing
2. Suspect
3. Prospect
4. Contato
5. Credibilidade
6. Oferta
7. Proposta
8. Negociação
9. Fechamento
10. Relacionamento
11. done!

Advancement signals:
- Credibilidade → Oferta: Client wants to know pricing/proposal
- Oferta → Proposta: Client requested formal proposal
- Proposta → Negociação: Client negotiating terms/price
- Negociação → Fechamento: Client confirmed purchase/signed contract

Rules:
- If clear advancement signal detected → suggest new stage
- If no clear signal → keep current stage
- Never skip stages (e.g., can't go Credibilidade → Proposta)
- User will confirm stage changes (you just suggest)

### 5. Status (blocked or moving?)
Options:
- `In progress`: User has action to take
- `Waiting Feedback`: Awaiting client response (no action for user now)
- `Scheduled`: Next action has scheduled date/time

Rules:
- If Next Action is something user must do → "In progress"
- If waiting on client decision/response → "Waiting Feedback"
- If next meeting is scheduled → "Scheduled"

### 6. AI Advancement Recommendation (strategic insights)
Format: 2-3 sentences with actionable strategic insights

**Purpose:** Provide pattern recognition and insights user might miss

**Categories to analyze:**
1. **Pattern Recognition:** Cliente mentioned X multiple times across interactions
2. **Opportunity Detection:** Ideal moment for Y based on behavior patterns
3. **Risk Warnings:** Stalling signals, competitive threats, timeline risks
4. **Blind Spots:** Validate budget/authority, prepare differentiation, timing constraints

Examples:
- "Cliente mencionou orçamento 3x e agora confirmou aprovação - momento ideal para proposta formal. Considere incluir case study similar."
- "Sem movimento há 21 dias mas cliente mantém interesse. Sugerir reunião objetiva para validar timeline."
- "Padrão: cliente avaliando 2 fornecedores. Destacar diferenciais técnicos na proposta."

Rules:
- Based on data patterns across interactions, not speculation
- Specific and actionable
- Can be empty if no significant insights
- Direct and consultative tone

## Output Format
Return ONLY a JSON object with these 6 fields:

```json
{
  "update_log": "DD/MM: [one sentence]",
  "next_action": "[verb + object]",
  "naction_due": "YYYY-MM-DD",
  "biz_funnel": "[current stage]" or "[suggested new stage]",
  "biz_funnel_changed": true/false,
  "biz_funnel_reasoning": "if changed, explain why in 1 sentence",
  "status": "In progress" | "Waiting Feedback" | "Scheduled",
  "ai_advancement_recommendation": "[2-3 sentences with strategic insights or empty string]"
}
```
```

**Success Criteria:**
- JSON válido retornado
- Todos os 6 campos preenchidos (ai_recommendation pode estar vazio)
- Update Log <= 10 palavras
- Next Action é acionável
- Biz Funnel está na lista de stages válidos
- AI Recommendation é específica e baseada em padrões detectados

---

### Step 5: Generate Output Report

**Objetivo:** Apresentar resultado para user validação

**Output Format (Markdown):**

```markdown
# Opportunity Update: {Opportunity Name}

**Last Interaction:** {meeting.date}
**Current Stage:** {current_biz_funnel} → {suggested_biz_funnel (if changed)}
**Priority:** {priority}

---

## Proposed Updates

### 1. Update Log
```
{proposed.update_log}
```

**Current:** {current.update_log (primeira linha)}

### 2. Next Action
```
{proposed.next_action}
```

**Current:** {current.next_action}

### 3. NAction Due
```
{proposed.naction_due}
```

**Current:** {current.naction_due}
**Rationale:** Last interaction + 7 days (or client deadline if mentioned)

### 4. Biz Funnel Stage
```
{proposed.biz_funnel}
```

**Current:** {current.biz_funnel}
**Changed:** {yes/no}
{if changed: **Reasoning:** {biz_funnel_reasoning}}

### 5. Status
```
{proposed.status}
```

**Current:** {current.status}

### 6. AI Advancement Recommendation ⭐
```
{proposed.ai_advancement_recommendation}
```

**Purpose:** Strategic insights from analyzing {N} recent interactions
**Based on:** Pattern recognition across multiple touchpoints

---

## Meeting Context Analyzed

**Interactions analyzed:** {N} recent meetings/interactions
**Key patterns detected:** {patterns extracted from multi-source analysis}
**Transcript excerpt (most recent):** {first 200 chars of transcript...}
**Summary:** {summary}
**Key Decisions:** {extracted from analysis}

---

## Next Steps

{if dry-run mode:}
This is a DRY RUN. No changes will be made to Notion.

To apply these changes, run:
`claude-code advance-opportunity "{opportunity_name}" --execute`

{if execute mode:}
Do you want to apply these updates to Notion? (yes/no)
```

---

## Update Execution (if --execute flag)

### Step 6: User Confirmation

**Prompt:**
```
Apply these updates to Notion?
[Y]es / [N]o / [E]dit / [S]kip field
```

**Options:**
- `Y`: Proceed with update
- `N`: Cancel, do not update
- `E`: Open interactive editor to adjust fields
- `S`: Skip specific fields (e.g., "skip biz_funnel" if user disagrees)

### Step 7: Update Notion via MCP

**MCP Tool:** `mcp__notion__notion-update-page`

**Update Command:**
```json
{
  "page_id": "{opportunity_page_id}",
  "command": "update_properties",
  "properties": {
    "Update Log": "{append_with_newline: proposed.update_log}",
    "Next Action": "{proposed.next_action}",
    "date:Next Action Date:start": "{proposed.next_action_date}",
    "date:Next Action Date:is_datetime": 0,
    "Biz Funnel": "{proposed.biz_funnel}",
    "Status": "{proposed.status}"
  }
}
```

**Note:** Update Log deve APPEND (não replace). Formato:
```
{proposed.update_log}
{existing.update_log}
```

**Success Criteria:**
- Update returns 200 OK
- Properties são refletidas no Notion

### Step 8: Confirmation Output

```markdown
✅ Opportunity updated successfully!

View in Notion: {opportunity_url}

**Updated fields:**
- Update Log: ✅
- Next Action: ✅
- Next Action Date: ✅
- Biz Funnel: {✅ or ⏭️ skipped}
- Status: ✅

**Next:** {proposed.next_action} by {proposed.next_action_date}
```

---

## Error Handling

### Common Errors

| Error | Cause | Recovery |
|-------|-------|----------|
| `OpportunityNotFound` | Nome não encontrado no database | Suggest similar names (fuzzy match) |
| `MultipleOpportunitiesFound` | >1 resultado com mesmo nome | Show list, ask user to choose |
| `NoMeetingData` | Anotações vazio E Offline Notes vazio | Error: "No meeting data to process" |
| `InvalidDate` | Não consegue extrair data da anotação | Usar created timestamp como fallback |
| `AIProcessingError` | Claude não retornou JSON válido | Retry 1x, se falhar pedir user input |
| `NotionUpdateError` | MCP update falhou | Log error, rollback se possível |

### Logging

**Create `logs/` folder**

**Log Format:** `logs/opportunity-advancer-{timestamp}.log`

**Log Contents:**
```
[YYYY-MM-DD HH:MM:SS] INFO: Starting Opportunity Advancer
[YYYY-MM-DD HH:MM:SS] INFO: Querying opportunity: "Projeto SiDi"
[YYYY-MM-DD HH:MM:SS] DEBUG: Found 1 result: {url}
[YYYY-MM-DD HH:MM:SS] INFO: Loading properties...
[YYYY-MM-DD HH:MM:SS] DEBUG: Properties: {json dump}
[YYYY-MM-DD HH:MM:SS] INFO: Loading 3 Anotações...
[YYYY-MM-DD HH:MM:SS] DEBUG: Most recent: {date} - {title}
[YYYY-MM-DD HH:MM:SS] INFO: Analyzing meeting content...
[YYYY-MM-DD HH:MM:SS] DEBUG: AI Response: {json}
[YYYY-MM-DD HH:MM:SS] INFO: Generated 5 fields successfully
[YYYY-MM-DD HH:MM:SS] INFO: User confirmed update
[YYYY-MM-DD HH:MM:SS] INFO: Updating Notion...
[YYYY-MM-DD HH:MM:SS] INFO: ✅ Success! Opportunity updated.
```

---

## Testing Plan

### Test Cases

#### Test Case 1: Standard Update (Happy Path)
- **Oportunidade:** {escolher uma real com reunião recente}
- **Expected:** Todos 5 campos gerados corretamente
- **Validation:** Comparar com output do Notion AI (se disponível)

#### Test Case 2: Stage Advancement
- **Oportunidade:** {uma que claramente avançou de stage}
- **Expected:** biz_funnel_changed = true, reasoning válido

#### Test Case 3: Waiting on Client
- **Oportunidade:** {uma onde está aguardando cliente}
- **Expected:** Status = "Waiting Feedback", Next Action não é ação do user

#### Test Case 4: Offline Notes Only
- **Oportunidade:** {uma sem Anotações mas com Offline Notes}
- **Expected:** Processar Offline Notes como fonte

#### Test Case 5: Multiple Anotações
- **Oportunidade:** {uma com 5+ anotações}
- **Expected:** Identificar corretamente a mais recente

### Acceptance Criteria

**Para considerar Fase 0 concluída:**
- [ ] Agent processa 5/5 test cases com sucesso
- [ ] Output é consistente com Notion AI (>80% match)
- [ ] Tempo de execução < 30 segundos
- [ ] User consegue validar e aplicar updates facilmente
- [ ] Logs são úteis para debug
- [ ] Error handling funciona para casos comuns

---

## Future Enhancements (Fora de Escopo Fase 0)

1. **Auto-detect new Anotações:** Polling ou webhook para executar automaticamente
2. **Batch processing:** Processar múltiplas oportunidades de uma vez
3. **Learning from corrections:** Track quando user edita output, aprender padrões
4. **Integration with Notion AI:** Sincronizar lógica entre ambos agents
5. **Confidence scores:** Mostrar confiança em cada campo (Low/Medium/High)

---

## Implementation Checklist

### Pre-Implementation
- [ ] Review esta spec completamente
- [ ] Validar que MCP tools estão disponíveis (`mcp__notion__notion-search`, `fetch`, `update-page`)
- [ ] Decidir: Python ou TypeScript? (Recomendação: use Claude Code capabilities diretamente)

### Implementation (Core)
- [ ] Setup project structure (`agents/`, `logs/`)
- [ ] Implement Step 1: Query Oportunidade
- [ ] Implement Step 2: Fetch Properties
- [ ] Implement Step 3: Load Most Recent Anotações
- [ ] Implement Step 4: AI Analysis
- [ ] Implement Step 5: Generate Output Report

### Implementation (Execution)
- [ ] Implement Step 6: User Confirmation
- [ ] Implement Step 7: Update Notion
- [ ] Implement Step 8: Success Message

### Testing & Refinement
- [ ] Run Test Case 1 (Standard Update)
- [ ] Run Test Case 2 (Stage Advancement)
- [ ] Run Test Case 3 (Waiting on Client)
- [ ] Run Test Case 4 (Offline Notes Only)
- [ ] Run Test Case 5 (Multiple Anotações)
- [ ] Compare with Notion AI output (3-5 examples)
- [ ] Refine analysis prompt based on results
- [ ] Document any deviations from spec

### Documentation
- [ ] Write README for agent (`docs/2.2-opportunity-advancer-usage.md`)
- [ ] Document command syntax
- [ ] Add troubleshooting section
- [ ] Add example outputs

---

## References

- **Process Documentation:** Notion `/Processos de Negócio - CRM Workflow - 2.2 - Avanço`
- **AI Configuration:** Notion `/My Notion AI`
- **Technical Architecture:** `docs/arquitetura.md`
- **Roadmap:** `docs/cronograma.md`

---

**Fim da Especificação**
**Versão:** 1.0
**Ready for Implementation:** ✅
